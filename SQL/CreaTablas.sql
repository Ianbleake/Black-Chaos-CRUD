use blackchaos 

create table Productos(id int primary key, Producto varchar(20), Precio float , Stock binary, Descripcion varchar(140))

create table Usuarios(Usuario varchar(20) primary key,Contraseña varchar(15))